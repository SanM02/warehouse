# ==========================================
# CONFIGURACIÓN DEL SISTEMA DE FERRETERÍA - V2
# ==========================================
# INSTRUCCIONES:
# 1. Copie este archivo y renómbrelo a ".env"
# 2. Reemplace los valores por defecto con sus valores reales
# 3. NUNCA suba el archivo .env a Git (ya está en .gitignore)

# ==========================================
# BASE DE DATOS POSTGRESQL (Docker)
# ==========================================
# Estos valores se usan para el contenedor de PostgreSQL
# IMPORTANTE: Cambie la contraseña en producción
DB_NAME=ferreteria_inventario
DB_USER=postgres
DB_PASSWORD=CAMBIE_ESTA_CONTRASEÑA_EN_PRODUCCION
DB_PORT=5432

# ==========================================
# DJANGO - SEGURIDAD
# ==========================================
# SECRET_KEY: Genere una clave única de 50+ caracteres
# Para generar: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-GENERE-UNA-CLAVE-UNICA-AQUI

# DEBUG: False en producción, True solo en desarrollo
DEBUG=False

# ALLOWED_HOSTS: Dominios permitidos separados por comas
ALLOWED_HOSTS=localhost,127.0.0.1

# ==========================================
# JWT - TOKENS DE AUTENTICACIÓN
# ==========================================
# Tiempo de vida del token de acceso (en horas)
JWT_ACCESS_TOKEN_LIFETIME_HOURS=8

# Tiempo de vida del token de refresco (en días)
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# ==========================================
# NOTAS IMPORTANTES
# ==========================================
# 
# ✅ El sistema tiene valores por defecto en docker-compose.yml
#    Si falta este archivo .env, usará defaults seguros
# 
# ⚠️  NUNCA use valores por defecto en producción
# 
# 💾 Haga backup de este archivo en un lugar seguro
#
# 🔐 Contraseña segura: mínimo 16 caracteres, mezcla de
#    mayúsculas, minúsculas, números y símbolos
#
# ==========================================
